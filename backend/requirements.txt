#Web Framework 

fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# Data Validation & Settings 

pydantic==2.6.0
pydantic-settings==2.1.0
python-dotenv==1.0.1

# HTTP Client (for inter-service comms)

httpx==0.26.0

# ML & Data 

#Note: In production Docker, torch is usually installed from a separate base image 
#to keep build times low. For local dev, we list it here.
#Ensure you are using the CUDA version if you have a GPU.

numpy==1.26.4
pandas==2.2.0
scikit-learn==1.4.0

# Utils 

#Structured logging is mentioned in your Architecture Report (Page 25)

structlog==24.1.0

# Database

sqlalchemy>=2.0
aiosqlite>=0.20